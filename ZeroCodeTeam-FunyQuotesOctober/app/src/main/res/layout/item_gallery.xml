<?xml version="1.0" encoding="utf-8"?>
<RelativeLayout xmlns:android="http://schemas.android.com/apk/res/android"
    android:layout_width="fill_parent"
    android:layout_height="@dimen/gallery_item_height">

    <ImageView
        android:id="@+id/image"
        android:layout_width="fill_parent"
        android:layout_height="fill_parent"
        android:adjustViewBounds="true"
        android:background="@drawable/stroke"
        android:cropToPadding="true"
        android:padding="@dimen/gallery_item_padding"
        android:scaleType="centerCrop" />

    <TextView
        android:id="@+id/tv_rating"
        style="@style/TextRating"
        android:layout_width="wrap_content"
        android:layout_height="wrap_content"
        android:layout_alignParentBottom="true"
        android:layout_alignParentRight="true"
        android:layout_marginBottom="@dimen/gallery_text_margin"
        android:layout_marginRight="@dimen/gallery_text_margin"
        android:paddingRight="@dimen/gallery_text_padding"
        android:text="+0"
        android:textStyle="bold" />

    <ImageView
        android:id="@+id/gif_sign_menu"
        android:layout_width="fill_parent"
        android:layout_height="fill_parent"
        android:adjustViewBounds="true"
        android:cropToPadding="true"
        android:padding="25dp"
        android:src="@drawable/ic_gif"
        android:visibility="gone" />
</RelativeLayout>